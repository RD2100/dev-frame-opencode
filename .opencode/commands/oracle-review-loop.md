# oracle-review-loop

Run multi-round GPT-Agent Review Loop.

```bash
python tools/oracle_gpt_review_loop.py --task-id s2 --max-rounds 3
```

Config: `_reports/gpt-review-loop/s2/LOOP_CONFIG.yaml`
State: `_reports/gpt-review-loop/s2/LOOP_STATE.json`
