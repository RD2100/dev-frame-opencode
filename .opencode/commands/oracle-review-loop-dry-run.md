# oracle-review-loop-dry-run

Dry-run the multi-round GPT-Agent Review Loop harness without submitting to GPT.

```bash
python tools/oracle_gpt_review_loop.py --task-id s2 --max-rounds 3 --dry-run true
```

Generates: `_reports/gpt-review-loop/s2/HARNESS_TEST_REPORT.md`
