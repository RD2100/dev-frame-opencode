# v1.2 Release — Stabilization & Trusted Chain

## Status: PASS (3/3 tracks complete)

### Track A: Unified Trust Verification
- `verify_run_evidence()` shared function with full tristate (evidence_ok, chain_trusted, final_report_consistent)
- Readiness gate: 3/3 probe + p95<60s + auth + proxy required for apply
- Readiness history: `runs/codex-readiness/history.jsonl`
- `run verify` and `goal_runner` share same function

### Track B: Goal Batch Closed Loop
- `goal_report.py` auto-generates `goal-report.md` + `goal-evidence.json`
- `goal_runner` uses `find_task(task_id).last_run_id` for precise run_id
- Batch schema: `batch_id → task_id → run_id` triplet in goal-evidence.json
- Goal acceptance: 18/18 PASS
- Real E2E: passed (see below)

### Track C: Documentation Convergence
- README updated: Claude Code = primary coding backend
- execution-policy.yaml: `coding_backend: claude`
- agent_client fallback default: `""` (no auto OpenCode)
- OpenCode: degraded optional, explicit `--backend opencode` only
- `docs/agent-onboarding.md`, `memory/` (19 cards), `CLAUDE.md`

## Real E2E Evidence

- **Goal ID**: `goal-20260525-072713-add--e2e-verification-marker-c`
- **Run ID**: `run-20260525-072735-0ea61d`
- **Status**: passed
- **Chain**: codex_cli (plan/review/finalize) + claude (exec/fix), all exit=0, no fallback
- **Diff**: `main.py` (+1 line: `# e2e verification marker`)
- **Evidence**: 10 artifacts (state.json, final-report, chain-evidence, diff.patch, review, safety, goal-report, goal-evidence)

## Acceptance

```
smoke:              11 PASS
dynamic:             8 PASS
goal:               18 PASS
chain-negative:      5 PASS (bad run correctly rejected)
```

## Known Limits

1. chain-truth stderr check too strict — tls handshake in Codex stderr flagged as ERROR
2. goal acceptance is monkeypatch logic suite — not real dynamic tests
3. Codex requires `HTTPS_PROXY=http://127.0.0.1:7897`
4. OpenCode degraded (1/6 stress pass), not auto fallback
5. No auto push/merge/deploy (policy all BLOCKED)
